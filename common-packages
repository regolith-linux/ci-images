# essentials
curl
jq
rsync
ssh
sudo
wget

# git related packages
ca-certificates
git
git-buildpackage

# gpg packages
gpg
gpg-agent

# packages required for building .deb package
devscripts
diffutils
dput
packaging-dev
